# 资源说明

当前确认原型没有引用独立的本地位图文件，因此本目录暂不包含 PNG、JPG 或 WebP 资源。

原型中的视觉资源均随 `index.html` 一起提交：

- 功能图标：JavaScript 中的内联 SVG 字符串；
- 项目封面、公众号文章封面和宣传图片候选：CSS 渐变 Mock；
- 微信授权二维码：JavaScript 生成的二维码样式 Mock；
- 微信状态栏与操作图标：HTML 字符和 CSS 样式。

这些资源只用于交互说明，不应直接作为生产素材。仓库根目录 `images/` 属于原有运营推广手册，与本原型无关，本次未修改或复用。
